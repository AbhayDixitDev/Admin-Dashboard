name: Serve Jekyll Siteon:  push:    branches:      - mainjobs:  serve:    runs-on: ubuntu-latest    steps:      - name: Checkout code        uses: actions/checkout@v2      - name: Install dependencies        run: bundle install      - name: Build and serve        env:          JEKYLL_ENV: production        run: |          bundle exec jekyll build          bundle exec jekyll serve
