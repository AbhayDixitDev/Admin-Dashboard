name: Serve JSON Serveron:  push:    branches:      - mainjobs:  serve:    runs-on: ubuntu-latest    steps:      - name: Checkout code        uses: actions/checkout@v2      - name: Install dependencies        run: npm install      - name: Start JSON Server        run: npm run start        env:          NODE_ENV: production          PORT: 8080
